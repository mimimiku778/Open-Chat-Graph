<VirtualHost *:80>
    ServerName ocgraph.test
    ServerAdmin webmaster@localhost

    # HTTPからHTTPSへリダイレクト
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
</VirtualHost>
