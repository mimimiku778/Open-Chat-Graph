# Mock環境の設定ファイル
# このファイルを編集してモック環境の動作を制御します

# データ件数（デフォルト: 1万件）
# - 100000: 本番並みの大量データ（デバッグ用）
# - 10000: 少量データ（CI・高速テスト用、推奨）
MOCK_RANKING_COUNT=10000
MOCK_RISING_COUNT=1000

# レスポンス遅延（デフォルト: 無効）
# - 1: 遅延あり（本番並みの処理時間をシミュレート、20-45分、デバッグ用）
# - 0: 遅延なし（CI・高速テスト用、推奨）
MOCK_DELAY_ENABLED=0

# モックAPIタイプ（デフォルト: dynamic）
# - dynamic: 動的データ（時間経過でデータが変化）
# - fixed: 固定データ（カテゴリテスト用、80件固定）
MOCK_API_TYPE=dynamic

# テストスクリプトの実行回数設定
# test-debug.sh（デバッグ用）および test-ci.sh（CI用）で使用
# 日本語: 25時間（デフォルト、23:30開始→翌23:30まで）
TEST_JA_HOURS=25
# 繁体字中国語: 1時間（デフォルト）
TEST_TW_HOURS=1
# タイ語: 1時間（デフォルト）
TEST_TH_HOURS=1

# 注意事項：
# - 設定変更後は `make restart-mock` で再起動が必要
# - test-*.sh スクリプトは .env.mock から設定を読み込む
